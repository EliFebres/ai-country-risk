# Repository ignore rules.
#
# Grouped by what the files are, not by the tool that made them. Two ordering
# rules matter and are easy to break by tidying:
#   * Comments must be on their own line. Git treats a trailing "# ..." as part
#     of the pattern, so an inline comment silently changes what is ignored.
#   * Re-includes ("!pattern") only win when they are the LAST matching line in
#     the file, so the negations live at the bottom. Do not move them up.


# --- Python bytecode and build artifacts -----------------------------------

__pycache__/
*.py[codz]
*$py.class

.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST


# --- Test and coverage output ----------------------------------------------

htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py.cover
.hypothesis/
.pytest_cache/
cover/


# --- Virtualenvs and secrets ------------------------------------------------
#
# `.env*` is deliberately broad: every real env file stays out, including
# `.env.local` and `.env.production`. The committed `.env.example` templates are
# re-admitted by the negations at the bottom of this file.

.env*
.envrc
.venv
env/
venv/
ENV/
env.bak/
venv.bak/


# --- Local tooling, not part of the project ---------------------------------

# Local agent/tooling config - never commit.
.claude/

# Jupyter autosaves of backend/walkthrough.ipynb and friends.
.ipynb_checkpoints

# Playwright MCP scratch logs.
.playwright-mcp/

.scrapy


# --- Generated and operator-local data under backend/data -------------------

# Fetched World Bank macro panels, one Parquet partition per country. Rebuilt on
# demand by the Step 0 backfill (walkthrough.ipynb) or by the daily pipeline, so
# there is nothing here worth versioning.
backend/data/wb_panel_wide

# The curated drop folder: CSVs an operator downloads by hand for the sources
# with no free, stable, no-auth API. Untracked deliberately, which also keeps
# the schema templates and their README out of a fresh clone - `curated_loader`
# will load zero rows there and every curated indicator reports as absent until
# someone recreates the folder. The last tracked copy of the README, which
# documents each file's schema and where to download it, is in git history at
# commit 66eae2b (backend/data/curated/README.md).
backend/data/curated

# Output of backend/tests/live_country_check.py.
live_check_*.log


# --- Re-includes: must stay last (see the header) ---------------------------

!backend/.env.example
!frontend/.env.example
